[project]
name = "maps-csv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Yehor Smoliakov", email = "yehors@ukr.net" }
]
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.13.4",
]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.3",
]
